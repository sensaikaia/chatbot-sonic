# Streamlit for the application UI
streamlit

# OpenAI API for LLM
openai

# Environment variable management
python-dotenv

# Document processing libraries
PyPDF2
python-docx
python-pptx

# LangChain core and related modules
langchain-openai
langchain-community

# Additional recommended libraries
langchain          # Core LangChain functionalities
pydantic           # Validation library used in LangChain (ensure compatibility)
faiss-cpu          # Vector store backend for embeddings (if not using Pinecone/Weaviate)
tiktoken           # Tokenization for OpenAI models
sentence-transformers  # Alternative embeddings provider
numpy              # Required for numerical operations in vector stores
scikit-learn       # Used for some LangChain utilities like clustering
